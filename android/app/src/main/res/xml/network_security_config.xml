<?xml version="1.0" encoding="utf-8"?>
<!-- 
  Network Security Configuration
  Developer: Hawkaye Visions LTD — Pakistan
  
  NOTE: For production, configure HTTPS domain and remove cleartext permissions.
-->
<network-security-config>
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system"/>
        </trust-anchors>
    </base-config>
    
    <!-- Allow cleartext for local development -->
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="true">10.0.2.2</domain>
        <domain includeSubdomains="true">localhost</domain>
    </domain-config>
    
    <!-- 
      Allow cleartext for EC2 backend (temporary, for development only)
      NOTE: For production, configure HTTPS with a proper domain name and SSL certificate.
    -->
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">13.127.85.109</domain>
    </domain-config>
</network-security-config>
